import React from "react";
import Link from "@mui/material/Link";

const MyLink = () => {
  return (
    <Link>
        sx={
            { fontSize: '0.8rem' }
        }
    </Link>
  );
};

export default MyLink;
